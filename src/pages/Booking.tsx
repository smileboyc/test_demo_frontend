import React from 'react';
import { Typography } from '@mui/material';

const Booking: React.FC = () => (
  <Typography variant="h4">Booking Page (Coming Soon)</Typography>
);

export default Booking; 